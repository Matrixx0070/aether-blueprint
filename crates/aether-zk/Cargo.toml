[package]
name = "aether-zk"
version = "0.41.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
# HH-B (2026-07-02): real Groth16 zk-SNARK over BN254 — the arkworks
# ecosystem. Trust assumption: arkworks is widely used (Aleo, Aztec,
# Zcash-adjacent tooling built on it) but 0.6.0 is a comparatively
# recent major version; documented per the same posture as the
# ed448-goldilocks pre-release note (Plan EE risk register pattern).
ark-groth16 = "0.6"
ark-bn254 = "0.6"
ark-relations = "0.6"
ark-ff = "0.6"
ark-std = "0.6"
ark-snark = "0.6"
ark-serialize = "0.6"
